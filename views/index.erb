<!DOCTYPE html>
<html>
  <head>
    <title>Hangman</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <link href="https://fonts.googleapis.com/css?family=Gloria+Hallelujah" rel="stylesheet">
  </head>
  <body>
    <h1>Hangman</h1>
    <p id="display">
      <% @display_word.each { |letter| %> <%=letter %> <% } %>
    </p>
    <p> 
      Guessed letters: 
      <% @guessed_letters.each { |letter| %> <%=letter %> <% } %>
    </p>
    <p> Guesses left: <%= @guesses_left %> </p>
    <form method="POST">
      <input type="text" name="guess" />
      <input id="button" type="submit" value="Guess!" />
    </form>
    <p id="message"> <%= @message %>
    <p>
      <% if win? || lose? %>
        <a href="/" class="btn">Play again?</a>
      <% end %>
    </p>
    <footer>
      <p>
        CS 673 Parth Yagnik
      </p>
      
    </footer>
  </body>
</html>
